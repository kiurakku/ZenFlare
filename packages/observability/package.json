{
  "name": "@zenflare/observability",
  "version": "0.1.0",
  "description": "ZenFlare Observability: one message instead of a thousand logs; Zen-Dashboard (waves/flames).",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node-dev src/index.ts",
    "start": "node dist/index.js",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
