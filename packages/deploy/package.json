{
  "name": "@zenflare/deploy",
  "version": "0.1.0",
  "description": "ZenFlare Deploy: one push, no Docker/CI — framework detection, build, host, light pulse.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node-dev src/index.ts",
    "start": "node dist/index.js",
    "lint": "tsc --noEmit",
    "test": "tsc && node --test dist/detect.test.js"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
